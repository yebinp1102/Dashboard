import Loading from '/assets/icons/loader.svg';

const Loader = () => {
  return (
    <div>
      <img 
        src={Loading}
        alt="loader"
        width={44}
        height={44}
      />
    </div>
  )
}

export default Loader